{
  "private": true,
  "scripts": {
    "svgo": "svgo --folder maps --precision 3 --output assets/maps; svgo --folder separators --precision 2 --output assets/separators",
    "start": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "cheerio": "1.0.0-rc.12",
    "prettier": "^2.8.1"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^2.0.1",
    "colorjs.io": "^0.4.2",
    "svgo": "^3.0.2"
  }
}
